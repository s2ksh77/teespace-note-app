import React from 'react';

import HeaderButtonGroup from '../../apps/note/components/common/buttons';

export default {
  title: 'Common/HeaderButtonGroup',
  component: HeaderButtonGroup,
};

const Template = (args) => <HeaderButtonGroup {...args} />;

export const ShowHeaderButtonGroup = Template.bind({});
ShowHeaderButtonGroup.args = {};

